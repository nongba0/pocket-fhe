# ARM Build & Cross-compilation scripts

이 폴더는 ARM(aarch64) 타겟 cross-compilation 및 QEMU runner 설정 스크립트를 포함합니다.

## 사용법

1. WSL Ubuntu 상에서 `aarch64-linux-gnu` cross compiler 및 `qemu-aarch64-static` 설치
2. `aarch64` 타겟 빌드 및 QEMU 상에서 PBS 샌드박스 연산 검증
